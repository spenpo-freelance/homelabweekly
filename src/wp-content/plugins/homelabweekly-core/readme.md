# Homelab Weekly Core

Custom plugin for Homelab Weekly. Holds CSS, JS, and `[homelabweekly_*]` shortcodes. Deployed by GitHub Actions rsync on push to `main`.

Do not install this from WordPress.org. After rsync, activate with MCP `plugin-activate` or wp-admin.
